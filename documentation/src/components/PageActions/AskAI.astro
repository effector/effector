---
import DropDownMenu from "../DropDownMenu.astro";
import ChevronDown from "@icons/ChevronDown.astro";

// Get current page URL for AI prompts
const currentUrl = new URL(Astro.url.pathname, Astro.site).href;
const prompt = `Read ${currentUrl}, I want to ask questions about it.`;
const encodedPrompt = encodeURIComponent(prompt);

// AI options with links
const aiLinks = [
  {
    text: { en: "ChatGPT", ru: "ChatGPT" },
    link: `https://chatgpt.com/?hints=search&q=${encodedPrompt}`,
  },
  {
    text: { en: "Claude", ru: "Claude" },
    link: `https://claude.ai/new?q=${encodedPrompt}`,
  },
];
---

<DropDownMenu links={aiLinks} class="ask-ai-dropdown">
  <span class="ask-ai-text">Ask AI</span>
</DropDownMenu>

<!-- <style>
  .ask-ai-dropdown {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.375rem 0.75rem;
    font-size: 0.875rem;
    font-weight: 500;
    line-height: 1.25rem;
    border-radius: 0.375rem;
    border: 1px solid var(--theme-divider);
    background-color: var(--theme-bg-offset);
    color: var(--theme-text);
    cursor: pointer;
    transition: all 0.15s ease-in-out;
    user-select: none;
  }

  .ask-ai-dropdown :global(.menu-button) {
    padding: 0;
    gap: 0.5rem;
  }

  .ask-ai-dropdown:hover {
    background-color: var(--theme-bg-hover);
    border-color: var(--theme-accent);
  }

  .ask-ai-text {
    font-size: 0.875rem;
    font-weight: 500;
  }

  /* Mobile adjustments */
  @media (max-width: 640px) {
    .ask-ai-dropdown {
      padding: 0.25rem 0.5rem;
      font-size: 0.813rem;
    }
  }
</style> -->
