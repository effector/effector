---
id: installation
title: Установка
description: Установка эффектора и сопутствующих пакетов
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs
  defaultValue="npm"
  groupId="package-manager"
  values={[
    {label: 'npm', value: 'npm'},
    {label: 'yarn', value: 'yarn'},
    {label: 'deno', value: 'deno'},
  ]}>
  <TabItem value="npm">

```bash
npm install effector
```

  </TabItem>
  <TabItem value="yarn">

```bash
yarn add effector
```

  </TabItem>
  <TabItem value="deno">

Можно импортировать `effector.mjs` с любого CDN, например так:

```typescript
import {createStore} from 'https://cdn.jsdelivr.net/npm/effector/effector.mjs'
```

:::note 
`effector.mjs` поддерживается начиная с effector 21.0.0
:::

  </TabItem>
</Tabs>

## Дополнительные пакеты

### Для react

<Tabs
  defaultValue="npm"
  groupId="package-manager"
  values={[
    {label: 'npm', value: 'npm'},
    {label: 'yarn', value: 'yarn'},
  ]}>
  <TabItem value="npm">

```bash
npm install effector effector-react
```

  </TabItem>
  <TabItem value="yarn">

```bash
yarn add effector effector-react
```

  </TabItem>
</Tabs>

### Для vue

<Tabs
  defaultValue="npm"
  groupId="package-manager"
  values={[
    {label: 'npm', value: 'npm'},
    {label: 'yarn', value: 'yarn'},
  ]}>
  <TabItem value="npm">

```bash
npm install effector effector-vue
```

  </TabItem>
  <TabItem value="yarn">

```bash
yarn add effector effector-vue
```

  </TabItem>
</Tabs>

### Для svelte

Svelte изначально работает с effector без дополнительных пакетов. [Пример игры](https://github.com/today-/citycatch) написанной с помощью svelte и effector

## DevTools

Пакет [effector-logger](https://github.com/effector/logger) позволяет выводить обновления в консоль и отображать текущие значения состояний в ui браузера, а также подключать приложение к redux devtools

## Online playground

Примеры в этой документации работают в [effector playground](https://share.effector.dev), который позволяет быстро тестировать идеи и делиться ими. Typescript и react поддерживаются из коробки

[Репозиторий проекта](https://github.com/effector/repl)

## Совместимость

Библиотека предоставляет отдельные модули с совместимостью вплоть до IE11 и Chrome 47 (браузера для Smart TV устройств):  `effector/compat`, `effector-react/compat` и `effector-vue/compat`

Использование с заменой импорта:

```diff
- import {createStore} from 'effector'
+ import {createStore} from 'effector/compat'
```

Использование с [babel-plugin-module-resolver](https://github.com/tleunen/babel-plugin-module-resolver):

```json title=".babelrc"
{
  "plugins": [
    ["babel-plugin-module-resolver", {
      "alias": {
        "^effector$": "effector/compat",
        "^effector-react$": "effector-react/compat"
      }
    }]
  ]
}
```
